apiVersion: batch/v1
kind: Job
metadata:
  name: did-sim
  labels:
    edu.web3/plugin-id: edu.hot.did
    edu.web3/lang-group: move
    edu.web3/compliance: testnet-only
spec:
  backoffLimit: 1
  template:
    metadata:
      labels:
        edu.web3/plugin-id: edu.hot.did
    spec:
      restartPolicy: Never
      containers:
        - name: runner
          image: edu/toolchain-move:0.1.0
          env:
            - name: CHAIN_ID
              value: "11155111"
            - name: TASK_TYPE
              value: "HOT_DID_PRIVACY_DEMO"
            - name: EDU_LANG_GROUP
              value: "move"
            - name: RPC_URL
              valueFrom:
                configMapKeyRef:
                  name: testnet-rpc-sepolia
                  key: rpc_url
          command:
            - sh
            - -c
            - |
              echo "[testnet] plugin=edu.hot.did task=${TASK_TYPE} chain=${CHAIN_ID}"
              echo "Simulation completed (educational only)"
