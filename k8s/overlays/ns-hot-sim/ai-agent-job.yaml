apiVersion: batch/v1
kind: Job
metadata:
  name: ai-agent-sim
  labels:
    edu.web3/plugin-id: edu.hot.ai-agent
    edu.web3/lang-group: evm
    edu.web3/compliance: testnet-only
spec:
  backoffLimit: 1
  template:
    metadata:
      labels:
        edu.web3/plugin-id: edu.hot.ai-agent
    spec:
      restartPolicy: Never
      containers:
        - name: runner
          image: edu/toolchain-evm:0.1.0
          env:
            - name: CHAIN_ID
              value: "11155111"
            - name: TASK_TYPE
              value: "HOT_AI_AGENT_SANDBOX"
            - name: EDU_LANG_GROUP
              value: "evm"
            - name: RPC_URL
              valueFrom:
                configMapKeyRef:
                  name: testnet-rpc-sepolia
                  key: rpc_url
          command:
            - sh
            - -c
            - |
              echo "[testnet] plugin=edu.hot.ai-agent task=${TASK_TYPE} chain=${CHAIN_ID}"
              echo "Simulation completed (educational only)"
